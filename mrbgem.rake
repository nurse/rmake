MRuby::Gem::Specification.new('rmake') do |spec|
  spec.license = 'MIT'
  spec.authors = 'naruse'
  spec.summary = 'gmake-compatible build engine in mruby'

  # Keep dependencies minimal; add as needed.
  # spec.add_dependency 'mruby-io'
end
